NODE_ENV=development
DATABASE_URL=postgresql://replaylab:replaylab_local@localhost:5432/replaylab
REDIS_URL=redis://localhost:6379
API_PORT=4000
# PORT overrides API_PORT on hosting providers.
SERVE_WEB=false
# Set to 1 only when all requests pass through your trusted reverse proxy.
TRUST_PROXY_HOPS=0
WEB_URL=http://localhost:5173
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=
ALLOW_PRIVATE_NETWORK_TARGETS=false
DEMO_PASSWORD=
AI_ENABLED=false
OPENAI_API_KEY=
OPENAI_MODEL=
